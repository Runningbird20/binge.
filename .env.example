# Frontend environment for local development or Vercel
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_PUBLISHABLE_KEY=

# Keep this false on Vercel until the legacy Express + SQLite backend is migrated
REACT_APP_ENABLE_LEGACY_BACKEND=false

# Optional: only set this if you host the legacy backend separately
# REACT_APP_API_URL=
