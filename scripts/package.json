{
  "name": "binge-catalog-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for media catalog sync to standalone PostgreSQL database",
  "dependencies": {
    "dotenv": "^16.0.0",
    "pg": "^8.20.0"
  }
}
